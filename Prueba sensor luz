int sensorLuz = A2;

void setup() {
  // put your setup code here, to run once:
 pinMode(sensorLuz, INPUT);
 Serial.begin(9600);
}

void loop() {
  // put your main code here, to run repeatedly:
  int leer = analogRead(sensorLuz);
  Serial.println(leer);
  delay(1000);
}
